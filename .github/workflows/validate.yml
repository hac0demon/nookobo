name: Validate

on:
  push:
  pull_request:

jobs:
  source-checks:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Install host checks
        run: sudo apt-get update && sudo apt-get install -y build-essential bash make binutils
      - name: Run repository checks
        run: make test
